# models/images/ — Image pipeline outputs

Trained fits and result metadata for the four models in `../../images/`.

| Subdirectory | Description |
|---|---|
| `fits/` | Trained model files (`.pkl` for the baseline, `.keras` for the CNNs). |
| `json/` | Result metadata (`*_meta.json`) loaded by `04_model_comparison.ipynb`. |
