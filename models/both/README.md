# models/both/ — Multimodal pipeline outputs

Trained base-model fits and result metadata for the fusion notebooks in `../../both/`.

| Subdirectory | Description |
|---|---|
| `fits/` | The two base models retrained on the joint split for fusion. |
| `json/` | Result metadata (`*_meta.json`) for the three fusion strategies. |
