# models/text/ — Text pipeline outputs

Trained fits and result metadata for the seven models in `../../text/`.

| Subdirectory | Description |
|---|---|
| `fits/` | Trained model files (`.pkl` for scikit-learn, `.keras` for Keras). |
| `json/` | Result metadata (`*_meta.json`) loaded by `04_model_comparison.ipynb`. |
